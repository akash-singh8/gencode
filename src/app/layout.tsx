import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "GenCode",
  description:
    "GenCode is an AI-powered online code editor and review platform that helps developers generate, write, and optimize code effortlessly. Leverage AI suggestions, code reviews, and multi-language support to enhance your coding efficiency and code quality.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={poppins.className}>{children}</body>
    </html>
  );
}
